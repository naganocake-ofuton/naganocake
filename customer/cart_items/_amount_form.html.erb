<td class="text-left">
  <%= attachment_image_tag( cart_item.item, :image, size: "55x43", fallback: "no_image.jpg") %>
  <%= cart_item.item.name %>
</td>
<td class="text-right">
  ¥<%= tax_price(cart_item.item.price).to_s(:delimited) %>
</td>
<td>
  <%= form_for(cart_item, url: customers_cart_item_path(cart_item), method: :patch , local: true)do |f| %>
    <%= f.number_field :amount, class: "quantity", size: 10, min: 1, id: "quantity_cart" %>
    <%= f.submit "変更", class: "btn btn-info" %>
  <% end %>
</td>
<td class="text-right" id='sub_price-<%= cart_item.id %>'>
  ¥<%= sub_price(cart_item).to_s(:delimited) %>
</td>
<td>
  <%= link_to " 削除する", customers_cart_item_path(cart_item), method: :delete, local: true, data: {confirm: "#{cart_item.item.name}を削除してよろしいですか？"}, class: "glyphicon glyphicon-trash btn btn-danger" %>
</td>